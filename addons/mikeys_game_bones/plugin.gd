@tool
extends EditorPlugin
# No editor UI of its own -- this file exists so the addon registers as a
# real, toggleable Godot plugin. The framework itself is plain GDScript
# classes (global class_name), usable whether or not this plugin is enabled.
